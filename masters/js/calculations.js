export function calculateSummary(master,state){
  return {
    affinity:{available:0,required:0,remaining:0},
    emblems:{available:0,required:0,remaining:0},
    manuscripts:{available:0,required:0,remaining:0}
  };
}
