Auto Planner v12:
- Added clear bilingual usage instructions inside the Auto Planner.
- Target-level lists now hide every level equal to or below the selected current level.
- The same filtering applies to Auto Planner and all building cards.
- Includes cache prevention with new asset filenames and version v12.0.0.
